<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home2</title>
    <style>
        /* Reset default margin and padding */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling with background and text color */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fa;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Title Styling */
        h1 {
            font-size: 2.5em;
            color: #2a3d66;
            margin-bottom: 15px;
            text-align: center;
        }

        h2 {
            font-size: 1.8em;
            color: #4b6cb7;
            margin-bottom: 30px;
            text-align: center;
        }

        /* Form container styling */
        .form-container {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
            text-align: center;
        }

        /* Label styling */
        label {
            font-size: 1.2em;
            margin-bottom: 8px;
            display: block;
            color: #333;
        }

        /* Input field styling */
        input {
            padding: 12px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1.1em;
            width: 100%;
            transition: border-color 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: #4b6cb7;
        }

        /* Submit button styling */
        button {
            padding: 14px;
            background-color: #2a3d66;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 1.2em;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #4b6cb7;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .form-container {
                padding: 20px;
                width: 90%;
            }

            h1 {
                font-size: 2em;
            }

            h2 {
                font-size: 1.5em;
            }

            button {
                font-size: 1em;
            }
        }
    </style>
</head>
<body>
    <h1>Welcome Hirer!</h1>
    <h2>Enter skills to be hired</h2>
    <div class="form-container">
        <form action="/home2" method="post">
            <label for="skill">Skill</label>
            <input type="text" id="skill" name="skill" placeholder="Enter the skill you're hiring for" required>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
